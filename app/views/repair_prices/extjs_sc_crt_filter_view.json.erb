{
 "success": true,
 "items": [
  {
   xtype: 'form',
   title: '', closable: false,
   flex: 1,
	layout: {
	    type: 'vbox',
	    align : 'stretch',
	    pack  : 'start',
	}, 
     defaults: {
     	 xtype: 'textfield',
     	 margin: '10 10 0 10',
     },	
     
    items: [       
    <%= raw extjs_std_combo_model('shipowner', @item, {:allowBlank=>true}, {:multiSelect => false}) -%>,
    ],
    
   buttons: [
	{
        text: '<i class="fa fa-th-large"></i> Applica',
        handler: function(){
        	fp = this.up('form');
            if(fp.getForm().isValid()){
               this.up('window').fireEvent('onConfirm', this.up('window'), fp);
            }
        }
    }
   ]    
    
        
  }
 ]
}